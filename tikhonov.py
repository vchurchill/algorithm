# tikhonov regularization parameter
#alpha = np.power(10, -12)
#I = np.matlib.identity(p)
#eqd1u1 = np.dot(np.dot(np.linalg.inv(alpha*I + np.dot(np.matrix.transpose(K3),K3)),np.matrix.transpose(K3)),q1u)
#eqd2u2 = np.dot(np.dot(np.linalg.inv(alpha*I + np.dot(np.matrix.transpose(K4),K4)),np.matrix.transpose(K4)),q2u)
